@model Party

<div class="modal fade" id="cancel-party-modal" data-backdrop="true" data-remote="false" tabindex="-1" role="dialog" aria-labelledby="cancel-party-modalLabel" aria-describedby="Heading" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="cancel-party-modalLabel">@Resources.Common.BackOffice_Parties_CancelConfirmation #@Model.PartyID.ToString()?</h4>
                <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                    <span class="sr-only">@Resources.Common.Close</span>
                </button>
            </div>
            <div class="modal-body">

                <h4>@Resources.Common.PartyDetails</h4>

                <div class="container-fluid">
                    <div class="row bottom-border">
                        <div class="col-md-12">
                            <h5>@Resources.Common.Host</h5>
                            <p>@Model.HostDisplayName</p>
                        </div>
                    </div>
                    <div class="row bottom-border">
                        <div class="col-md-12">
                            <h5>@Resources.Common.Date</h5>
                            <p>@Model.StartDate.ToString()</p>
                        </div>
                    </div>
                </div>

            </div>
            <div class="modal-footer">
                @using (Ajax.BeginForm("CloseParty", new AjaxOptions { OnSuccess = "closeparty.success", OnFailure = "closeparty.failure" }))
                {
                    <input type="hidden" name="partyid" value="@Model.PartyID" />

                    <button class="btn btn-danger" type="submit">@Resources.Common.CancelParty</button>
                }
            </div>
        </div>
    </div>
</div>